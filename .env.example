# Prisma datasource connection string. Copy to .env and fill in real values —
# .env is gitignored and must never be committed.
DATABASE_URL="postgresql://<user>:<password>@<host>:<port>/<database>?schema=public"
